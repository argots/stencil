This file contains the standard way to download nodejs distributions.

{{ $os := stencil.OS }}
{{ $arch := stencil.Arch }}
{{if (eq stencil.Arch "amd64") }}
  {{ $arch = "x64" }}
{{else if (eq stencil.Arch "386") }}
  {{ $arch = "x86" }}
{{end}}

{{ $url := printf "https://nodejs.org/dist/v12.16.2/node-v12.16.2-%s-%s.tar.gz" $os $arch }}
{{ $bin := printf "node-v12.16.2-%s-%s/bin/node" $os $arch }}
{{ stencil.CopyFromArchive "nodejs" "./bin/node"  $url $bin }}
